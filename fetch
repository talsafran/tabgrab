#!/usr/bin/env ruby

require './lib/ultimate_guitar'
require 'yaml'

include UltimateGuitar

letter = ArtistLetter.new(ARGV[0])
tabs = letter.artists.map(&:tabs)

Logger.log("Found #{tabs.flatten.count} tabs.")
Logger.log("Saving YAML dump.")
File.open("log/dumps/#{ARGV[0]}.yml", 'w') { |f| f.write(letter.artists.to_yaml) }
